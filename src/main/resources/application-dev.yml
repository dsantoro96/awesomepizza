server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  application:
    name: awesomepizza
  data:
    mongodb:
      uri: mongodb://root:admin@localhost:27017
      database: awesomepizza
